# Tasks

1. Implement `greet(name: &str) -> String`
2. Implement `add(a: i32, b: i32) -> i32`
3. Implement `is_even(n: i32) -> bool`
4. Implement `factorial(n: u64) -> u64`
5. Implement `describe_option<T: Display>(opt: Option<T>) -> String`
