# Tasks

1. Implement `spawn_join() -> i32` (thread::spawn + join)
2. Implement `channel_example() -> Vec<i32>` (mpsc channel)
3. Implement `shared_counter() -> i32` (Arc<Mutex<...>>)
4. Implement `async_hello() -> String` (async fn)
5. Implement `run_tasks() -> (String, String)` (tokio::join!)
