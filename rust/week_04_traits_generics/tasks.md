# Tasks

1. Implement `Summary` trait for `Article` (`summarize() -> String`)
2. Implement `largest<T: PartialOrd>(list: &[T]) -> &T`
3. Implement `Display` for `Person2`
4. Implement `Debug` for `Person2`
