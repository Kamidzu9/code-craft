# Tasks

1. Implement `longest<'a>(x: &'a str, y: &'a str) -> &'a str`
2. Implement `Excerpt<'a>` struct holding `&'a str`
3. Implement `parse_num(s: &str) -> Result<i32, ParseIntError>`
4. Implement `read_file_str(path: &str) -> Result<String, io::Error>`
5. Implement `MyError` with `Display`
