# Notizen - Aufgabe 2: rectangle_area

`f64` ist der Typ für Gleitkommazahlen (Dezimalzahlen):

```rust
fn rectangle_area(width: f64, height: f64) -> f64 {
    width * height
}
```

Mit `f64` kannst du normale Rechenoperationen wie `+`, `-`, `*`, `/` durchführen.
