# Tasks

1. Implement `deref_raw() -> i32` (raw pointer dereference)
2. Implement `call_c_add(a: i32, b: i32) -> i32` (extern C)
3. Implement `say_hello!` macro (prints "Hello!")
4. Implement `use_macro()` (calls say_hello!)
5. Implement `static_mut_inc() -> u32` (static mut)
6. Implement `safe_divide(a: i32, b: i32) -> Option<i32>`
