import Foundation

// 1. age >= 18
func canVote(age: Int) -> Bool {
    fatalError("TODO: Gib true zurück wenn age >= 18, sonst false")
}

// 2. "A"/"B"/"C"/"D"/"F" je nach Punktzahl
func grade(score: Int) -> String {
    fatalError("TODO: Gib Note zurück: 90+ → A, 80+ → B, 70+ → C, 60+ → D, sonst F")
}

// 3. Nur "admin"/"secret" erlaubt
func login(username: String, password: String) -> Bool {
    fatalError("TODO: Gib true zurück nur wenn username == \"admin\" UND password == \"secret\"")
}

// 4. Absoluter Wert (Betrag) von n
func absolute(n: Int) -> Int {
    fatalError("TODO: Gib den Betrag von n zurück (immer positiv)")
}

// 5. switch: "dog" → "woof", "cat" → "meow", default → "unknown"
func animalSound(animal: String) -> String {
    fatalError("TODO: Verwende switch um das passende Tiergeräusch zurückzugeben")
}
