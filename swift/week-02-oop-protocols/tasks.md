# Tasks

1. Define a `class` `Car` with properties and methods.
2. Create a `struct` `Book` and compare value vs reference.
3. Define a `protocol` `Drivable` with a `drive()` method.
4. Make `Car` conform to `Drivable`.
5. Use `extension` to add functionality to a built-in type.
