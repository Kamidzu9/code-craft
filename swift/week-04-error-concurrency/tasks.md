# Tasks

1. Define an `enum` conforming to `Error`.
2. Write a `throws` function and handle with `do/catch`.
3. Use `defer` to clean up resources.
4. Write an `async` function and call it with `await`.
5. Use `async let` to run two tasks concurrently.
