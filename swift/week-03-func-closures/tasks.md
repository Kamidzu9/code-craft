# Tasks

1. Write a function that takes a closure as a parameter.
2. Use trailing closure syntax with `map` on an array.
3. Capture values in a closure and observe capture lists.
4. Create an `enum` with associated values.
5. Use `switch` with pattern matching on enums.
