# Tasks

1. Create a `Text` view and style it.
2. Add a `Button` that changes a `@State` variable.
3. Use `VStack`, `HStack`, `ZStack` to arrange views.
4. Build a `List` with dynamic rows.
5. Add a `TextField` and bind it to a `@State` property.
