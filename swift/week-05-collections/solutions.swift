import Foundation

// 1. Erstes Element oder nil bei leerem Array
func firstElement(arr: [Int]) -> Int? {
    fatalError("TODO: Gib das erste Element zurück, oder nil wenn arr leer ist")
}

// 2. Neues Array mit val am Ende
func addElement(arr: [Int], val: Int) -> [Int] {
    fatalError("TODO: Gib ein neues Array zurück, das alle Elemente + val am Ende enthält")
}

// 3. Jedes Element × 2
func double(arr: [Int]) -> [Int] {
    fatalError("TODO: Gib ein Array zurück, bei dem jedes Element verdoppelt wurde")
}

// 4. Größtes Element (mit Schleife)
func findMax(arr: [Int]) -> Int? {
    fatalError("TODO: Finde das größte Element in arr mit einer Schleife, gib nil bei leerem Array")
}

// 5. Nur gerade Zahlen
func evenNumbers(arr: [Int]) -> [Int] {
    fatalError("TODO: Gib ein neues Array nur mit den geraden Zahlen aus arr zurück")
}
