import Foundation

// 1. "Hello, {name}!"
func sayHello(name: String) -> String {
    fatalError("TODO: Gib \"Hello, \\(name)!\" zurück")
}

// 2. width * height
func rectangleArea(width: Double, height: Double) -> Double {
    fatalError("TODO: Gib width * height zurück")
}

// 3. true wenn num > 0
func isPositive(num: Int) -> Bool {
    fatalError("TODO: Gib true zurück wenn num > 0, sonst false")
}

// 4. Größere der beiden
func maxNum(a: Int, b: Int) -> Int {
    fatalError("TODO: Gib den größeren Wert von a und b zurück")
}

// 5. "hot" wenn > 30, "warm" wenn > 15, "cold" sonst
func describeTemperature(temp: Int) -> String {
    fatalError("TODO: Gib \"hot\", \"warm\" oder \"cold\" je nach temp zurück")
}
