import Foundation

// 1. Array [1, 2, ..., n]
func countTo(n: Int) -> [Int] {
    fatalError("TODO: Erstelle ein Array mit Zahlen von 1 bis n und gib es zurück")
}

// 2. 1+2+...+n
func sumTo(n: Int) -> Int {
    fatalError("TODO: Addiere alle Zahlen von 1 bis n und gib die Summe zurück")
}

// 3. Array [n, ..., 1] (absteigend)
func countdown(n: Int) -> [Int] {
    fatalError("TODO: Erstelle ein Array mit Zahlen von n bis 1 (absteigend)")
}

// 4. true wenn val in arr vorkommt
func contains(arr: [Int], val: Int) -> Bool {
    fatalError("TODO: Prüfe mit einer Schleife ob val in arr ist, gib true/false zurück")
}

// 5. [1*n, 2*n, ..., 10*n]
func multiplyTable(n: Int) -> [Int] {
    fatalError("TODO: Erstelle ein Array mit [1*n, 2*n, ..., 10*n]")
}
