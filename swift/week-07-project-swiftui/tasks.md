# Tasks

1. Build a list-detail app with `NavigationStack`.
2. Fetch data from a public API with `URLSession`.
3. Display the data in a `List` with async image loading.
4. Add pull-to-refresh and error states.
5. Add a detail view that receives the selected item.
