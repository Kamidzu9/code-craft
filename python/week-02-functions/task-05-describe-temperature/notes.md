# Task 5: describe_temperature(temp)

Mit `if`/`elif`/`else` kannst du mehrere Bedingungen prüfen.

```python
def describe_temperature(temp):
    if temp > 30:
        return "hot"
    elif temp > 15:
        return "warm"
    else:
        return "cold"
```
