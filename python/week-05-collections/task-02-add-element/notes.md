# Task 2: add_element(arr, val)

Erstelle eine Kopie der Liste und hänge `val` an.

```python
def add_element(arr, val):
    neue_liste = arr.copy()
    neue_liste.append(val)
    return neue_liste
```
