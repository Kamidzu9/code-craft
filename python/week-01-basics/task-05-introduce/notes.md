# Task 5: introduce(name, age)

f-Strings (`f"..."`) erlauben es, Variablen direkt in Strings einzubetten.

```python
def introduce(name, age):
    return f"Hi, I'm {name} and I'm {age} years old!"
```
