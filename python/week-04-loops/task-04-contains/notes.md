# Task 4: contains(arr, val)

Iteriere über die Liste und vergleiche jedes Element.

```python
def contains(arr, val):
    for element in arr:
        if element == val:
            return True
    return False
```
