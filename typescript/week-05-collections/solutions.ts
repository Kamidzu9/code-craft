// === Woche 5: Arrays & Collections ===

/**
 * Gibt das erste Element zurück, oder null wenn das Array leer ist.
 */
export function firstElement(arr: number[]): number | null {
  // TODO: Gib arr[0] zurück, oder null wenn das Array leer ist
}

/**
 * Gibt ein neues Array mit val am Ende zurück.
 */
export function addElement(arr: number[], val: number): number[] {
  // TODO: Erstelle eine Kopie von arr mit val am Ende
}

/**
 * Gibt ein neues Array zurück, bei dem jedes Element mit 2 multipliziert wurde.
 */
export function double(arr: number[]): number[] {
  // TODO: Multipliziere jedes Element mit 2
}

/**
 * Findet das größte Element im Array (ohne Math.max).
 */
export function findMax(arr: number[]): number | null {
  // TODO: Finde das größte Element mit einer Schleife
}

/**
 * Gibt ein neues Array nur mit den geraden Zahlen zurück.
 */
export function evenNumbers(arr: number[]): number[] {
  // TODO: Behalte nur die geraden Zahlen
}
