# Tasks

1. Implement `getStatus(s: Status): string`
2. Implement `combine(a: A, b: B): A & B`
3. Implement `isString(val: unknown): val is string`
4. Implement `setPort(p: 3000 | 3001 | 8080): number`
5. Implement `handleShape(s: Shape): number`
