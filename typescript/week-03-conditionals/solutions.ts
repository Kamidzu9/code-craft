// === Woche 3: Bedingungen (Conditionals) ===

/**
 * Gibt true zurück, wenn age >= 18 ist.
 */
export function canVote(age: number): boolean {
  // TODO: Gib true zurück wenn age >= 18, sonst false
}

/**
 * Gibt eine Note zurück: 90+ → "A", 80+ → "B", 70+ → "C", 60+ → "D", sonst "F".
 */
export function grade(score: number): string {
  // TODO: Prüfe den score und gib die passende Note zurück
}

/**
 * Gibt true zurück, wenn username = "admin" UND password = "secret".
 */
export function login(username: string, password: string): boolean {
  // TODO: Prüfe ob username === "admin" und password === "secret"
}

/**
 * Gibt den absoluten Wert von n zurück (ohne Math.abs).
 */
export function absolute(n: number): number {
  // TODO: Wenn n negativ ist, gib -n zurück, sonst n
}

/**
 * Gibt den Tierlaut zurück: "dog" → "woof", "cat" → "meow", sonst "unknown".
 */
export function animalSound(animal: string): string {
  // TODO: Verwende switch/case für die Tierlaute
}
