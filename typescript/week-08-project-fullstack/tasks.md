# Tasks

1. Implement `UserCardProps interface (user: User)`
2. Implement `fetchUsers(): Promise<User[]>`
3. Implement `LoginForm component with validation`
4. Implement `useApi<T>(url: string): { data: T | null; loading: boolean }`
5. Implement `appReducer(state: AppState, action: AppAction): AppState`
