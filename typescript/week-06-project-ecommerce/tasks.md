# Tasks

1. Implement `calcTotal(items: CartItem[]): number`
2. Implement `validateOrder(order: Order): boolean`
3. Implement `applyDiscount(price: number, discount: Discount): number`
4. Implement `checkStock(product: Product, qty: number): boolean`
5. Implement `Product type (id, name, price, stock)`
