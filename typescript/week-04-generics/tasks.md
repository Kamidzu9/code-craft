# Tasks

1. Implement `identity<T>(arg: T): T`
2. Implement `createResponse<T>(data: T): { data: T; error: null }`
3. Implement `getLength<T extends { length: number }>(arg: T): number`
4. Implement `push(item: T): void (on Stack<T>)`
5. Implement `updatePartial<T>(obj: T, patch: Partial<T>): T`
