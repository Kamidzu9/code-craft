export function createTeam(name: string, members: string[]): { name: string; members: string[]; count: number } {
  // TODO: Gib { name, members, count: members.length } zurück
  return { name: "", members: [], count: 0 };
}
