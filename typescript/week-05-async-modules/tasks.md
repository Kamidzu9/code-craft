# Tasks

1. Implement `fetchJSON(url: string): Promise<unknown>`
2. Implement `export const PI: number`
3. Implement `waitAll<T>(promises: Promise<T>[]): Promise<T[]>`
4. Implement `logMethod decorator (log method calls)`
5. Implement `EventEmitter class with on/emit/off`
