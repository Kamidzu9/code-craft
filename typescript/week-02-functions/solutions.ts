// === Woche 2: Funktionen ===
// Schreibe DEINEN Code in jede Funktion.

/**
 * Gibt "Hello, {name}!" zurück.
 */
export function sayHello(name: string): string {
  // TODO: Gib "Hello, " + name + "!" zurück
}

/**
 * Berechnet die Fläche eines Rechtecks (width * height).
 */
export function rectangleArea(width: number, height: number): number {
  // TODO: Gib width * height zurück
}

/**
 * Gibt true zurück, wenn num > 0 ist.
 */
export function isPositive(num: number): boolean {
  // TODO: Gib true zurück wenn num > 0, sonst false
}

/**
 * Gibt die größere der beiden Zahlen zurück.
 */
export function max(a: number, b: number): number {
  // TODO: Gib a zurück wenn a > b, sonst b
}

/**
 * Gibt "hot" zurück wenn temp > 30, "warm" wenn temp > 15, sonst "cold".
 */
export function describeTemperature(temp: number): string {
  // TODO: Prüfe die Temperatur und gib den passenden String zurück
}
