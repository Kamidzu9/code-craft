# sayHello() – Parameter in Strings einbetten

Mit Template Literals kannst du den Parameter direkt in den String einfügen.

```typescript
function sayHello(name: string): string {
  return `Hello, ${name}!`;
}
```
