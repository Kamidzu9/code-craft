# rectangleArea() – Rechnen mit Parametern

Multipliziere zwei Zahlen miteinander und gib das Ergebnis zurück.

```typescript
function rectangleArea(width: number, height: number): number {
  return width * height;
}
```
