// === Woche 4: Schleifen (Loops) ===

/**
 * Gibt ein Array [1, 2, 3, ..., n] zurück.
 */
export function countTo(n: number): number[] {
  // TODO: Erstelle ein Array mit Zahlen von 1 bis n
}

/**
 * Berechnet die Summe 1 + 2 + ... + n.
 */
export function sumTo(n: number): number {
  // TODO: Addiere alle Zahlen von 1 bis n und gib die Summe zurück
}

/**
 * Gibt ein Array [n, ..., 2, 1] zurück.
 */
export function countdown(n: number): number[] {
  // TODO: Erstelle ein Array mit Zahlen von n runter bis 1
}

/**
 * Gibt true zurück, wenn val im Array arr vorkommt.
 */
export function contains(arr: number[], val: number): boolean {
  // TODO: Durchsuche das Array nach val
}

/**
 * Gibt ein Array [1*n, 2*n, ..., 10*n] zurück.
 */
export function multiplyTable(n: number): number[] {
  // TODO: Erstelle ein Array mit den ersten 10 Vielfachen von n
}
