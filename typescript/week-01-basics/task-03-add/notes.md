# add() – Funktionen mit Parametern

Funktionen können Parameter (Eingabewerte) entgegennehmen. Jeder Parameter bekommt einen Typ.

```typescript
function add(a: number, b: number): number {
  return a + b;
}
```

`a` und `b` sind vom Typ `number`, der Rückgabewert ist ebenfalls `number`.
