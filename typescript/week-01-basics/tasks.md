# Tasks

1. Implement `declare variable `name: string` with value "TypeScript"`
2. Implement `add(a: number, b: number): number`
3. Implement `createUser(id: number, name: string, email: string): User`
4. Implement `greetUser(user: User): string`
5. Implement `formatId(id: string | number): string`
