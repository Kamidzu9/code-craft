# Tasks

1. Implement `createApp(): ExpressApp`
2. Implement `addRoutes(app: ExpressApp): void`
3. Implement `validateBody(body: unknown): body is { name: string }`
4. Implement `errorHandler(err: Error): { status: number; message: string }`
5. Implement `signToken(payload: object): string`
