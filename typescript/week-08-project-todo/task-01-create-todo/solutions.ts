let nextId = 1;

export function createTodo(text: string): { id: number; text: string; done: boolean } {
  // TODO: Gib ein Todo-Objekt mit id, text und done: false zurück
  return { id: 0, text: "", done: false };
}
