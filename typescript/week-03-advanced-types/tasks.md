# Tasks

1. Implement `makeOptional<T>(obj: T): Partial<T>`
2. Implement `isArrayType<T>(val: unknown): val is T[]`
3. Implement `prependUser(id: string): `user_${string}``
4. Implement `pickKeys<T, K extends keyof T>(obj: T, keys: K[]): Pick<T, K>`
5. Implement `DeepReadonly<T> type and freeze<T>(obj: T): DeepReadonly<T>`
